import { SxProps, Theme } from '@material-ui/system';

export default {
  root: {
    width: '100%',
    minHeight: '300px',
    background: '#FFF',
    boxShadow: '0px 2px 2px rgba(0, 0, 0, 0.1)',
    borderRadius: '6px',
  },
} as {
  root: SxProps<Theme>;
};
