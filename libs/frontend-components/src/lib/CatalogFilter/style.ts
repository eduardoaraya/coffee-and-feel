import { SxProps, Theme } from '@material-ui/system';

export default {
  root: {
    boxShadow: 1,
    width: '100%',
    minHeight: '300px',
    background: '#FFF',
  },
} as {
  root: SxProps<Theme>;
};
