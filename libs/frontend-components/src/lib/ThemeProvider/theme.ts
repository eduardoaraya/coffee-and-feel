import { createTheme } from '@material-ui/core';

const defaultTheme = createTheme({
  palette: {
    primary: {
      main: '#323232',
    },
    secondary: {
      main: '#8953E3',
    },
  },
});

export default defaultTheme;
