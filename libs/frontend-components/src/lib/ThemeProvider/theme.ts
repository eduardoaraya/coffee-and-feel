import { createTheme } from '@material-ui/core';

const defaultTheme = createTheme({
  palette: {
    primary: {
      main: '#8953E3',
    },
    secondary: {
      main: '#323232',
    },
  },
});

export default defaultTheme;
