import { LayoutStore } from '@atlascode/coffee-front-components';

/* eslint-disable-next-line */
export interface HomePageProps {}

export const HomePage: React.FC = (props: HomePageProps): JSX.Element => {
  return <LayoutStore />;
};

export default HomePage;
